get:
  tags:
    - tickets
  summary: Get ticket details by id
  operationId: getTicketById
  description: >
    Те же query-параметры поиска билетов, что и в GET /tickets (контекст tickets-service).
    Фронт повторяет их при открытии карточки для PackService и аналитики.
    JWT в Authorization опционален: при валидном токене в Kafka уходит user_type=AUTH и user_id.

  security:
    - bearerAuth: []

  parameters:
    - $ref: '../components/parameters.yaml#/XAnonymousId'
    - $ref: '../components/parameters.yaml#/XGenerationId'
    - $ref: '../components/parameters.yaml#/XPackRevision'
    - $ref: '../components/parameters.yaml#/XGenerationMode'
    - $ref: '../components/parameters.yaml#/XRequestId'
    - $ref: '../components/parameters.yaml#/XTicketsRevision'

    - name: id
      in: path
      required: true
      description: Tripify ticket identifier (tid)
      schema:
        type: string
      example: ticket_8f3a1c2d

    - $ref: '../components/parameters.yaml#/OriginCityCode'
    - $ref: '../components/parameters.yaml#/DestinationCityCode'
    - $ref: '../components/parameters.yaml#/DepartureDate'
    - $ref: '../components/parameters.yaml#/ReturnDate'
    - $ref: '../components/parameters.yaml#/Currency'
    - $ref: '../components/parameters.yaml#/Adults'
    - $ref: '../components/parameters.yaml#/BudgetMaxAmount'
    - $ref: '../components/parameters.yaml#/Children'
    - $ref: '../components/parameters.yaml#/Infants'

  responses:
    '200':
      description: Ticket details fetched successfully
      content:
        application/json:
          schema:
            $ref: '../components/schemas/TicketDetailResponse.yaml'

    '404':
      $ref: '../components/responses.yaml#/NotFound'

    '500':
      $ref: '../components/responses.yaml#/InternalServerError'
